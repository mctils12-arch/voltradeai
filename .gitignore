node_modules/
dist/
*.db
*.sqlite
__pycache__/
*.pyc
.env

.pre_release_backup/

# backtest price cache (regenerated on demand)
.bt_cache/

# visual harness output (screenshots regenerated per run)
.visual/

# test_auto_discovery.py runtime artifact
.test_manifest.json

# tank-fill v2/v3 master chips (binary ~2MB/scene; regenerable from CDSE —
# committed metadata lives in datacore/sentinel2/*chips_index.jsonl)
datacore/sentinel2/chips/
datacore/sentinel2/chips_s1/

# Claude Code session worktrees (background agent isolation copies)
.claude/worktrees/

# ROOT VALIDATION LADDER gate-2 script outputs (session-local re-run
# artifacts; the script is the permanent, reproducible artifact, not its
# JSON output -- results are recorded in prose in research/experiments.md
# and research/open_questions.md, same convention already followed for
# form4_gate2_results.json/cot_gate2 outputs before this file existed)
*_gate2_results.json
