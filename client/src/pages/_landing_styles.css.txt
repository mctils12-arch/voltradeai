.vt-landing-root, .vt-landing-root .vt-landing-root {
    --bg: #050a13;
    --bg-2: #0a1628;
    --surface: #0f1d33;
    --surface-2: #142543;
    --hairline: rgba(120,165,220,0.08);
    --hairline-2: rgba(120,165,220,0.16);
    --ink: #eef3fb;
    --ink-2: #b3c2d8;
    --muted: #6680a0;
    --muted-2: #364762;
    /* Primary brand */
    --alpha: #4d9fff;
    --alpha-bright: #7cc4ff;
    --alpha-soft: rgba(77,159,255,0.18);
    /* Semantic finance colors */
    --gain: #4ade80;        /* green — buys / positive */
    --gain-soft: rgba(74,222,128,0.14);
    --loss: #ff5a6e;        /* red — sells / negative */
    --loss-soft: rgba(255,90,110,0.14);
    --ai: #c084fc;          /* purple — AI / intelligence / Pro */
    --ai-soft: rgba(192,132,252,0.14);
    --warn: #fbb24c;        /* amber — warnings / highlights */
    --warn-soft: rgba(251,178,76,0.14);
    --gold: #f0b860;
    --sans: 'Geist', system-ui, -apple-system, sans-serif;
    --mono: 'Geist Mono', ui-monospace, monospace;
  }

  .vt-landing-root *,
  .vt-landing-root { box-sizing: border-box; margin: 0; padding: 0; }
  .vt-landing-root { background: var(--bg); color: var(--ink); font-family: var(--sans); -webkit-font-smoothing: antialiased; }
  .vt-landing-root { min-height: 100vh; overflow-x: hidden; }
  a { color: inherit; text-decoration: none; }
  button { font-family: inherit; cursor: pointer; border: none; background: none; color: inherit; }

  .container { max-width: 1280px; margin: 0 auto; padding: 0 32px; position: relative; z-index: 2; }
  @media (max-width: 720px) { .container { padding: 0 20px; } }

  /* ============== NAV ============== */
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    border-bottom: 1px solid transparent;
    backdrop-filter: blur(14px) saturate(180%);
    -webkit-backdrop-filter: blur(14px) saturate(180%);
    background: rgba(5,10,19,0.55);
    transition: border-color .3s, background .3s;
  }
  nav.scrolled { border-bottom-color: var(--hairline); background: rgba(5,10,19,0.85); }
  .nav-inner { display: flex; align-items: center; justify-content: space-between; height: 64px; padding: 0 32px; max-width: 1280px; margin: 0 auto; }
  @media (max-width: 720px) { .nav-inner { padding: 0 20px; } }
  .logo { font-size: 19px; font-weight: 700; letter-spacing: -0.03em; display: flex; align-items: center; gap: 2px; }
  .logo .vol, .logo .trade { color: var(--ink); }
  .logo .ai {
    color: var(--alpha); font-family: var(--mono); font-size: 10px; font-weight: 500;
    margin-left: 6px; padding: 2px 6px; border-radius: 4px; background: var(--alpha-soft);
    letter-spacing: 0.06em; text-transform: uppercase;
  }
  .nav-links { display: flex; gap: 32px; font-size: 14px; color: var(--ink-2); }
  .nav-links a { transition: color .15s; }
  .nav-links a:hover { color: var(--ink); }
  .nav-cta { display: flex; align-items: center; gap: 10px; }
  .btn-login {
    display: inline-flex; align-items: center;
    padding: 9px 18px; border-radius: 8px;
    font-size: 14px; font-weight: 500;
    color: var(--ink);
    border: 1px solid var(--hairline-2);
    background: rgba(15,29,51,0.4);
    transition: border-color .15s, background .15s;
  }
  .btn-login:hover { border-color: var(--ink-2); background: rgba(15,29,51,0.7); }
  .btn {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 10px 18px; border-radius: 8px; font-size: 14px; font-weight: 500;
    transition: transform .15s ease, background .15s, color .15s, border-color .15s;
  }
  .btn-primary { background: var(--alpha); color: #061328; font-weight: 600; box-shadow: 0 0 0 0 rgba(77,159,255,0); transition: transform .15s ease, background .15s, box-shadow .25s; }
  .btn-primary:hover { transform: translateY(-1px); background: var(--alpha-bright); box-shadow: 0 0 32px rgba(77,159,255,0.35); }
  .btn-xl { padding: 16px 28px; font-size: 15px; font-weight: 700; letter-spacing: 0.02em; border-radius: 10px; }
  .btn-ghost { border: 1px solid var(--hairline-2); color: var(--ink); background: rgba(15,29,51,0.4); backdrop-filter: blur(8px); }
  .btn-ghost:hover { border-color: var(--ink-2); }
  .btn .arrow { transition: transform .15s; }
  .btn:hover .arrow { transform: translateX(2px); }
  @media (max-width: 880px) { .nav-links { display: none; } }

  /* Hero badge pill — LunarCrush-style, with icon + label */
  .hero-badge {
    display: inline-flex; align-items: center; gap: 8px;
    padding: 7px 14px;
    border-radius: 100px;
    background: rgba(77,159,255,0.08);
    border: 1px solid rgba(77,159,255,0.25);
    color: #7cc4ff;
    font-family: var(--mono);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    margin-bottom: 32px;
  }
  .hero-badge svg { width: 14px; height: 14px; }

  /* ============== HERO with 2-col layout: text + product mockup ============== */
  .hero {
    position: relative;
    min-height: 100vh;
    overflow: hidden;
    background: var(--bg);
    display: flex;
    align-items: center;
    padding: 120px 0 80px;
    z-index: 1;
  }
  /* Mobile: hero shouldn't be a full viewport. The headline must be near the top
     so it lands above the fold instead of getting flex-centered into a giant void. */
  @media (max-width: 720px) {
    .hero {
      min-height: auto;
      align-items: flex-start;
      padding: 96px 0 56px;
    }
  }
  /* Faint world map background — kept very subtle so the mockup is the centerpiece */
  .hero-bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    opacity: 0.45;
  }
  #world-canvas { position: absolute; inset: 0; width: 100%; height: 100%; }
  .hero-vignette {
    position: absolute; inset: 0;
    background:
      radial-gradient(900px 600px at 10% 50%, rgba(5,10,19,0.85) 0%, transparent 60%),
      radial-gradient(1200px 700px at 100% 100%, rgba(5,10,19,0.6) 0%, transparent 70%);
    pointer-events: none;
    z-index: 2;
  }
  .hero-grid {
    position: relative;
    z-index: 5;
    display: block;
  }
  /* Hero text: left-aligned, max-width contained, sits on top of the world map */
  .hero-text-centered {
    position: relative;
    z-index: 5;
    max-width: 720px;
    margin: 0;
  }

  /* ============== PRODUCT SHOWCASE: NVDA mockup, full-width below hero ============== */
  .product-showcase { padding: 80px 0 100px; }
  .showcase-eyebrow-text {
    font-family: var(--mono); font-size: 12px; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--muted);
    padding-top: 12px; border-top: 1px solid var(--hairline-2);
    width: max-content; margin-bottom: 24px;
  }
  .showcase-title {
    font-weight: 700; font-size: clamp(34px, 4.5vw, 56px);
    line-height: 1.05; letter-spacing: -0.035em;
    margin-bottom: 56px; max-width: 900px;
  }
  .showcase-title .accent { color: var(--alpha); }
  .showcase-mockup-wrap {
    max-width: 900px;
    margin: 0 auto;
  }

  /* ============== HERO PRODUCT MOCKUP ============== */
  .hero-mockup {
    position: relative;
  }
  .mockup-window {
    background: linear-gradient(180deg, rgba(15,29,51,0.85), rgba(10,22,40,0.92));
    border: 1px solid var(--hairline-2);
    border-radius: 14px;
    overflow: hidden;
    box-shadow:
      0 30px 80px rgba(0,0,0,0.5),
      0 0 0 1px rgba(124,196,255,0.04) inset,
      0 0 60px rgba(77,159,255,0.08);
    backdrop-filter: blur(20px);
  }
  .mockup-bar {
    display: flex; align-items: center; gap: 12px;
    padding: 12px 16px;
    border-bottom: 1px solid var(--hairline);
    background: rgba(5,10,19,0.4);
  }
  .mockup-dots { display: flex; gap: 6px; }
  .mockup-dots span {
    width: 10px; height: 10px; border-radius: 50%;
    background: var(--muted-2);
  }
  .mockup-dots span:nth-child(1) { background: #ff5a6e; }
  .mockup-dots span:nth-child(2) { background: #fbb24c; }
  .mockup-dots span:nth-child(3) { background: #4ade80; }
  .mockup-url {
    font-family: var(--mono); font-size: 12px;
    color: var(--muted); flex: 1;
    text-align: center;
  }
  .mockup-url strong { color: var(--ink); font-weight: 500; }

  .mockup-body { padding: 24px; }
  .mockup-head {
    display: flex; align-items: flex-start;
    justify-content: space-between;
    margin-bottom: 18px;
    padding-bottom: 18px;
    border-bottom: 1px solid var(--hairline);
  }
  .mockup-sym { display: flex; gap: 12px; align-items: center; }
  .sym-mark {
    font-family: var(--mono);
    font-weight: 600; font-size: 16px;
    padding: 6px 10px;
    background: var(--alpha-soft);
    color: var(--alpha-bright);
    border: 1px solid rgba(77,159,255,0.25);
    border-radius: 6px;
    letter-spacing: 0.05em;
  }
  .sym-name { font-size: 13px; color: var(--ink-2); margin-bottom: 3px; }
  .sym-price { font-size: 18px; font-weight: 600; color: var(--ink); }
  .sym-price .up { font-size: 12px; color: var(--gain); margin-left: 6px; font-weight: 500; }

  .mockup-verdict { text-align: right; }
  .mockup-verdict .vlabel {
    font-family: var(--mono); font-size: 10px;
    letter-spacing: 0.16em; color: var(--muted);
    text-transform: uppercase; margin-bottom: 4px;
  }
  .mockup-verdict .vval {
    font-size: 17px; font-weight: 600;
    color: var(--alpha-bright);
  }
  .mockup-verdict .vsize {
    font-family: var(--mono); font-size: 11px;
    color: var(--muted); margin-top: 4px;
  }

  .mockup-spark { margin: 0 -8px 18px; }
  .mockup-spark svg { width: 100%; height: 60px; display: block; }

  .mockup-vars { display: flex; flex-direction: column; gap: 10px; }
  .mvar { display: grid; grid-template-columns: 90px 1fr 90px; gap: 12px; align-items: center; }
  .mvar-l {
    font-family: var(--mono); font-size: 11px;
    letter-spacing: 0.08em; color: var(--ink-2);
    text-transform: uppercase;
  }
  .mvar-track {
    height: 5px; background: var(--bg-2);
    border-radius: 999px; overflow: hidden;
    border: 1px solid var(--hairline);
  }
  .mvar-fill {
    height: 100%; background: var(--gain);
    border-radius: 999px;
    transition: width 1s cubic-bezier(.2,.6,.2,1);
  }
  .mvar-fill.warn { background: var(--warn); }
  .mvar-fill.loss { background: var(--loss); }
  .mvar-v {
    font-family: var(--mono); font-size: 11px;
    text-align: right;
    color: var(--ink);
  }
  .mvar-v.up { color: var(--gain); }
  .mvar-v.warn { color: var(--warn); }

  .mockup-foot {
    margin-top: 18px;
    padding-top: 16px;
    border-top: 1px solid var(--hairline);
    display: flex;
    justify-content: space-between;
    font-family: var(--mono); font-size: 11px;
    color: var(--muted);
  }
  .mockup-foot strong { color: var(--ink-2); font-weight: 500; }
  .foot-r { color: var(--alpha); }

  .cities { position: absolute; inset: 0; pointer-events: none; transform-style: preserve-3d; }
  .city { position: absolute; transform-style: preserve-3d; }
  .city-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--alpha-bright);
    box-shadow: 0 0 12px var(--alpha-bright), 0 0 24px rgba(124,196,255,0.4);
    margin-left: -4px; margin-top: -4px;
    position: relative;
  }
  .city-dot::after {
    content: '';
    position: absolute; inset: -6px;
    border: 1px solid rgba(124,196,255,0.4);
    border-radius: 50%;
    animation: cityPulse 2.5s ease-out infinite;
  }
  @keyframes cityPulse {
    0% { transform: scale(0.8); opacity: 1; }
    100% { transform: scale(2.6); opacity: 0; }
  }
  .city-label {
    position: absolute;
    font-family: var(--mono);
    font-size: 10px;
    letter-spacing: 0.12em;
    color: var(--alpha-bright);
    white-space: nowrap;
    text-shadow: 0 0 8px rgba(5,10,19,0.95);
    opacity: 0.8;
  }
  /* Default position: top-right of the dot */
  .city-label.pos-tr { top: -22px; left: 8px; }
  /* Top-left: above and to the left */
  .city-label.pos-tl { top: -22px; right: 8px; left: auto; }
  /* Bottom-right: below and to the right */
  .city-label.pos-br { top: 12px; left: 8px; }
  /* Bottom-left: below and to the left */
  .city-label.pos-bl { top: 12px; right: 8px; left: auto; }
  /* Right: dead right */
  .city-label.pos-r { top: -5px; left: 12px; }
  /* Left: dead left */
  .city-label.pos-l { top: -5px; right: 12px; left: auto; }
  /* Top: directly above */
  .city-label.pos-t { top: -18px; left: 50%; transform: translateX(-50%); }
  /* Bottom: directly below */
  .city-label.pos-b { top: 12px; left: 50%; transform: translateX(-50%); }
  .city-building {
    position: absolute;
    bottom: 0; left: -2px;
    width: 4px;
    background: linear-gradient(0deg, var(--alpha-bright) 0%, var(--alpha) 60%, transparent 100%);
    transform-origin: bottom center;
    transform: rotateX(-90deg) scaleY(0);
    transform-style: preserve-3d;
    height: 90px;
    opacity: 0;
    box-shadow: 0 0 12px rgba(124,196,255,0.5);
  }
  .city.major .city-building { width: 6px; height: 140px; }

  .arcs-svg { position: absolute; inset: 0; width: 100%; height: 100%; pointer-events: none; overflow: visible; }

  .arc-path {
    fill: none;
    stroke: rgba(124, 196, 255, 0.4);
    stroke-width: 0.6;
    stroke-linecap: round;
    opacity: 0.6;
  }
  .arc-path.faint { stroke: rgba(77, 159, 255, 0.5); opacity: 0.4; }

  .data-particle {
    fill: #7cc4ff;
    filter: drop-shadow(0 0 4px rgba(124,196,255,0.9)) drop-shadow(0 0 8px rgba(124,196,255,0.6));
  }
  .data-particle.quote { fill: #7cc4ff; filter: drop-shadow(0 0 4px rgba(124,196,255,0.9)); }
  .data-particle.options { fill: #b6ff4a; filter: drop-shadow(0 0 4px rgba(182,255,74,0.9)); }
  .data-particle.news { fill: #f0b860; filter: drop-shadow(0 0 4px rgba(240,184,96,0.9)); }
  .data-particle.macro { fill: #d4a3ff; filter: drop-shadow(0 0 4px rgba(212,163,255,0.9)); }
  .data-particle.trade { fill: #ff8b4d; filter: drop-shadow(0 0 4px rgba(255,139,77,0.9)); }
  .data-particle.burst {
    fill: #ffffff;
    filter: drop-shadow(0 0 6px #ffffff) drop-shadow(0 0 14px rgba(255,255,255,0.8));
  }
  .particle-trail {
    fill: none;
    stroke-linecap: round;
    opacity: 0.55;
  }

  .stream-label {
    font-family: var(--mono);
    font-size: 9px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    fill: #b3c2d8;
    opacity: 0.6;
  }
  .stream-label.quote { fill: #7cc4ff; }
  .stream-label.options { fill: #b6ff4a; }
  .stream-label.news { fill: #f0b860; }
  .stream-label.macro { fill: #d4a3ff; }
  .stream-label.trade { fill: #ff8b4d; }

  .arrival-pulse {
    fill: none;
    stroke: #7cc4ff;
    stroke-width: 1.5;
    opacity: 0;
  }

  /* ============== STAGE TEXT ============== */
  .stage-text {
    position: absolute; inset: 0;
    z-index: 10;
    display: flex; align-items: center;
    pointer-events: none;
  }
  .stage {
    width: 100%;
    padding: 0 32px;
    max-width: 1280px;
    margin: 0 auto;
    opacity: 0;
    position: absolute;
    left: 50%;
    transform: translate(-50%, 20px);
    transition: opacity .6s ease, transform .6s ease;
    pointer-events: auto;
  }
  .stage.in {
    opacity: 1;
    transform: translate(-50%, 0);
  }
  @media (max-width: 720px) { .stage { padding: 0 20px; } }

  .eyebrow {
    display: inline-flex; align-items: center; gap: 10px;
    font-family: var(--mono); font-size: 11px; letter-spacing: 0.14em;
    text-transform: uppercase; color: var(--ink-2);
    padding: 6px 12px; border: 1px solid var(--hairline);
    border-radius: 999px; margin-bottom: 28px;
    background: rgba(15,29,51,0.6);
    backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px);
  }
  .pulse {
    width: 6px; height: 6px; border-radius: 50%;
    background: var(--alpha-bright);
    box-shadow: 0 0 0 0 var(--alpha-bright);
    animation: pulse 1.8s infinite;
  }
  @keyframes pulse {
    0% { box-shadow: 0 0 0 0 rgba(124,196,255,0.55); }
    70% { box-shadow: 0 0 0 8px rgba(124,196,255,0); }
    100% { box-shadow: 0 0 0 0 rgba(124,196,255,0); }
  }

  h1.headline {
    font-weight: 700; font-size: clamp(48px, 7.5vw, 96px);
    line-height: 0.95; letter-spacing: -0.045em;
    color: var(--ink); margin-bottom: 28px;
    max-width: 720px;
  }
  h1.headline .accent { color: var(--alpha); }
  @media (max-width: 720px) {
    h1.headline {
      font-size: clamp(40px, 11vw, 56px);
      line-height: 1;
      letter-spacing: -0.035em;
      margin-bottom: 20px;
    }
  }

  .stage-sub {
    font-size: 19px; line-height: 1.55; color: var(--ink-2);
    max-width: 540px; margin-bottom: 36px;
  }
  @media (max-width: 720px) { .stage-sub { font-size: 16px; } }

  .stage-cta { display: flex; gap: 12px; flex-wrap: wrap; align-items: center; }
  .stage-cta .meta { margin-left: 8px; font-size: 13px; font-family: var(--mono); color: var(--muted); }

  .stage h2.story-title {
    font-weight: 700;
    font-size: clamp(36px, 5vw, 64px);
    line-height: 1;
    letter-spacing: -0.04em;
    margin-bottom: 16px;
    max-width: 580px;
  }
  .stage h2.story-title .accent { color: var(--alpha); }

  /* ============== FIGURE IN DISTANCE ============== */
  .figure-overlay {
    position: absolute;
    inset: 0;
    z-index: 8;
    pointer-events: none;
    opacity: 0;
    transition: opacity 1s ease;
  }
  .figure-overlay.in { opacity: 1; }

  /* The figure is small and centered in distance, slightly above center */
  .figure-svg {
    position: absolute;
    bottom: 35%;
    left: 50%;
    transform: translateX(-50%);
    width: 140px;
    height: auto;
    filter: drop-shadow(0 0 12px rgba(77,159,255,0.4));
  }

  /* Asymmetric prose-led section block — replaces 3-col icon card grids */
  .prose-grid {
    display: grid;
    grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
    column-gap: 80px;
    row-gap: 24px;
    align-items: start;
  }
  @media (max-width: 980px) {
    .prose-grid { grid-template-columns: 1fr; column-gap: 0; row-gap: 28px; }
  }
  .prose-eyebrow-text {
    font-family: var(--mono); font-size: 12px; letter-spacing: 0.16em;
    text-transform: uppercase; color: var(--muted);
    grid-column: 1; grid-row: 1;
    padding-top: 12px;
    border-top: 1px solid var(--hairline-2);
    width: max-content;
  }
  .prose-grid > h2.section-title {
    grid-column: 1; grid-row: 2;
    margin-bottom: 0;
  }
  .prose-body {
    grid-column: 2; grid-row: 1 / span 2;
    align-self: end;
    color: var(--ink-2);
    font-size: 17px; line-height: 1.65;
  }
  @media (max-width: 980px) {
    .prose-eyebrow-text { grid-column: 1; grid-row: 1; }
    .prose-grid > h2.section-title { grid-column: 1; grid-row: 2; }
    .prose-body { grid-column: 1; grid-row: 3; }
  }
  .prose-body p { margin-bottom: 18px; }
  .prose-body p:last-child { margin-bottom: 0; }
  .prose-body strong { color: var(--ink); font-weight: 600; }
  .prose-body .prose-note {
    font-size: 14px; color: var(--muted);
    padding-top: 16px; border-top: 1px solid var(--hairline);
    margin-top: 6px;
  }
  section.s {
    padding: 120px 0;
    position: relative;
    border-bottom: 1px solid var(--hairline);
    z-index: 2;
    background: var(--bg);
  }
  section.s:last-of-type { border-bottom: none; }
  @media (max-width: 720px) { section.s { padding: 80px 0; } }

  /* ============== HOW IT WORKS — three-step (compact, three-column) ============== */
  .steps-section { background: var(--bg); }
  .steps-eyebrow {
    font-family: var(--mono); font-size: 11px; letter-spacing: 0.18em;
    text-transform: uppercase; color: var(--alpha);
    margin-bottom: 20px;
    padding: 6px 10px; background: var(--alpha-soft);
    border-radius: 4px;
    display: inline-block;
  }
  .steps-title {
    font-weight: 700;
    font-size: clamp(32px, 4.4vw, 56px);
    line-height: 1.05;
    letter-spacing: -0.035em;
    color: var(--ink);
    margin-bottom: 56px;
    max-width: 720px;
  }
  .steps-title .accent { color: var(--alpha); }
  .steps-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }
  .step-card {
    padding: 32px 28px;
    border: 1px solid var(--hairline-2);
    border-radius: 14px;
    background: rgba(15,29,51,0.35);
    position: relative;
    transition: border-color .2s ease, transform .2s ease;
  }
  .step-card:hover {
    border-color: rgba(77,159,255,0.45);
    transform: translateY(-2px);
  }
  .step-num {
    font-family: var(--mono);
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.16em;
    color: var(--alpha);
    margin-bottom: 20px;
  }
  .step-headline {
    font-size: 22px;
    font-weight: 700;
    color: var(--ink);
    letter-spacing: -0.015em;
    margin-bottom: 12px;
    line-height: 1.2;
  }
  .step-body {
    font-size: 15px;
    color: var(--ink-2);
    line-height: 1.6;
    margin: 0;
  }
  /* Mobile: stack the three cards vertically, tighten spacing */
  @media (max-width: 880px) {
    .steps-grid {
      grid-template-columns: 1fr;
      gap: 14px;
    }
    .steps-title { margin-bottom: 36px; }
    .step-card { padding: 24px 22px; }
    .step-headline { font-size: 19px; }
    .step-body { font-size: 14.5px; }
  }
  /* Tablet: 2 columns is awkward for a 3-step; jump to 1 column under desktop width */
  @media (min-width: 881px) and (max-width: 1024px) {
    .steps-grid { gap: 18px; }
    .step-card { padding: 28px 24px; }
  }

  .section-eyebrow {
    font-family: var(--mono); font-size: 11px; letter-spacing: 0.18em;
    text-transform: uppercase; color: var(--alpha);
    margin-bottom: 24px; display: inline-flex; align-items: center; gap: 8px;
    padding: 6px 10px; border-radius: 4px; background: var(--alpha-soft);
  }
  /* Semantic eyebrow color variants */
  .section-eyebrow.eyebrow-gain { color: var(--gain); background: var(--gain-soft); }
  .section-eyebrow.eyebrow-ai   { color: var(--ai);   background: var(--ai-soft);   }
  .section-eyebrow.eyebrow-warn { color: var(--warn); background: var(--warn-soft); }
  .section-eyebrow.eyebrow-loss { color: var(--loss); background: var(--loss-soft); }
  h2.section-title {
    font-weight: 700; font-size: clamp(40px, 5.5vw, 64px);
    line-height: 1; letter-spacing: -0.035em;
    margin-bottom: 24px; max-width: 900px;
  }
  h2.section-title .accent { color: var(--alpha); }
  .section-lede { font-size: 18px; line-height: 1.6; color: var(--ink-2); max-width: 660px; }

  /* TICKER */
  .ticker {
    border-top: 1px solid var(--hairline); border-bottom: 1px solid var(--hairline);
    padding: 14px 0; overflow: hidden;
    background: var(--bg-2); position: relative; z-index: 2;
  }
  .ticker-track {
    display: flex; gap: 48px; animation: scroll 50s linear infinite;
    white-space: nowrap; width: max-content;
  }
  .ticker-item { display: flex; align-items: baseline; gap: 10px; font-family: var(--mono); font-size: 13px; }
  .ticker-item .sym { color: var(--ink); font-weight: 500; }
  .ticker-item .pct.up { color: var(--alpha-bright); }
  .ticker-item .pct.down { color: var(--loss); }
  @keyframes scroll { from { transform: translateX(0); } to { transform: translateX(-50%); } }

  /* CARDS */
  .col3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; margin-top: 64px; }
  @media (max-width: 880px) { .col3 { grid-template-columns: 1fr; } }
  .card {
    background: linear-gradient(180deg, var(--surface) 0%, var(--bg-2) 100%);
    border: 1px solid var(--hairline); border-radius: 14px;
    padding: 32px; position: relative; overflow: hidden;
    transition: border-color .25s, transform .25s;
  }
  .card:hover { border-color: var(--hairline-2); transform: translateY(-2px); }
  .card-glyph {
    width: 44px; height: 44px; border-radius: 10px;
    background: var(--alpha-soft); border: 1px solid var(--hairline-2);
    display: grid; place-items: center;
    margin-bottom: 24px; color: var(--alpha);
  }
  .card-glyph svg { width: 20px; height: 20px; }
  .card h3 {
    font-weight: 600; font-size: 22px; letter-spacing: -0.025em;
    margin-bottom: 12px; line-height: 1.15;
  }
  .card p { color: var(--ink-2); line-height: 1.55; font-size: 15px; }
  .card .tag {
    position: absolute; top: 24px; right: 24px;
    font-family: var(--mono); font-size: 10px; letter-spacing: 0.12em;
    text-transform: uppercase; color: var(--muted);
  }
  /* Semantic tag variants — filled pills with color */
  .card .tag.tag-gain {
    color: var(--gain); background: var(--gain-soft);
    padding: 4px 8px; border-radius: 4px;
    border: 1px solid rgba(74,222,128,0.25);
  }
  .card .tag.tag-ai {
    color: var(--ai); background: var(--ai-soft);
    padding: 4px 8px; border-radius: 4px;
    border: 1px solid rgba(192,132,252,0.25);
  }
  .card .tag.tag-warn {
    color: var(--warn); background: var(--warn-soft);
    padding: 4px 8px; border-radius: 4px;
    border: 1px solid rgba(251,178,76,0.25);
  }

  /* DECISION CARD */
  .viz-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 56px; align-items: center; margin-top: 56px; }
  @media (max-width: 980px) { .viz-grid { grid-template-columns: 1fr; gap: 32px; } }
  .viz-grid h3 {
    font-weight: 700; font-size: clamp(28px, 3.5vw, 40px);
    letter-spacing: -0.03em; line-height: 1.1; margin-bottom: 18px;
  }
  .viz-grid p { color: var(--ink-2); font-size: 16px; line-height: 1.6; margin-bottom: 16px; }

  .decision {
    background: linear-gradient(180deg, var(--surface) 0%, var(--bg-2) 100%);
    border: 1px solid var(--hairline); border-radius: 18px;
    overflow: hidden; position: relative;
  }
  .decision::before {
    content: ''; position: absolute; inset: 0;
    background: radial-gradient(800px 400px at 90% 0%, var(--alpha-soft), transparent 60%);
    pointer-events: none;
  }
  .decision-head {
    padding: 22px 24px; border-bottom: 1px solid var(--hairline);
    display: flex; align-items: center; justify-content: space-between;
    background: rgba(10,22,40,0.5); position: relative;
  }
  .decision-head .ttl { display: flex; align-items: center; gap: 12px; }
  .decision-head .sym { font-weight: 700; font-size: 22px; letter-spacing: -0.02em; }
  .decision-head .name { font-family: var(--mono); font-size: 12px; color: var(--muted); }
  .verdict {
    font-family: var(--mono); font-size: 11px; letter-spacing: 0.12em;
    text-transform: uppercase; padding: 6px 12px; border-radius: 6px;
    background: rgba(77,159,255,0.14); color: var(--alpha-bright);
    border: 1px solid rgba(77,159,255,0.25);
  }
  .var-list { padding: 8px 0; position: relative; }
  .var-row {
    display: grid; grid-template-columns: 110px 1fr auto;
    gap: 16px; padding: 14px 24px; border-bottom: 1px solid var(--hairline);
    align-items: center;
  }
  .var-row:last-child { border-bottom: none; }
  .var-row .lbl {
    font-family: var(--mono); font-size: 10px; letter-spacing: 0.12em;
    text-transform: uppercase; color: var(--muted);
  }
  .var-bar { height: 6px; background: var(--bg-2); border-radius: 999px; overflow: hidden; position: relative; }
  .var-fill {
    position: absolute; top: 0; left: 0; bottom: 0;
    border-radius: 999px; background: var(--gain);
    transition: width 1s cubic-bezier(.2,.6,.2,1);
  }
  .var-fill.warn { background: var(--warn); }
  .var-fill.loss { background: var(--loss); }
  .var-row .val {
    font-family: var(--mono); font-size: 12px; color: var(--ink);
    min-width: 50px; text-align: right;
  }
  .var-row .val.up   { color: var(--gain); }
  .var-row .val.warn { color: var(--warn); }
  .var-row .val.loss { color: var(--loss); }
  .decision-foot {
    padding: 16px 24px; border-top: 1px solid var(--hairline);
    background: rgba(10,22,40,0.5);
    font-family: var(--mono); font-size: 11px; color: var(--muted);
    display: flex; align-items: center; gap: 8px;
  }
  .decision-foot strong { color: var(--ink-2); font-weight: 500; }

  /* PRICING */
  .pricing { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-top: 56px; }
  @media (max-width: 880px) { .pricing { grid-template-columns: 1fr; } }
  .price-card {
    background: linear-gradient(180deg, var(--surface) 0%, var(--bg-2) 100%);
    border: 1px solid var(--hairline); border-radius: 16px;
    padding: 36px; position: relative; transition: border-color .25s;
  }
  .price-card.featured {
    border-color: var(--alpha);
    background: linear-gradient(180deg, var(--surface-2) 0%, var(--bg-2) 100%);
  }
  .price-card.featured::before {
    content: ''; position: absolute; inset: 0;
    border-radius: 16px;
    background: radial-gradient(600px 300px at 50% 0%, var(--alpha-soft), transparent 70%);
    pointer-events: none;
  }
  .price-tag {
    display: inline-flex;
    font-family: var(--mono); font-size: 10px; letter-spacing: 0.14em;
    text-transform: uppercase; padding: 5px 10px; border-radius: 4px;
    margin-bottom: 16px; color: var(--muted);
    border: 1px solid var(--hairline-2);
  }
  .price-card.featured .price-tag {
    color: var(--alpha); background: var(--alpha-soft); border-color: transparent;
  }
  .price-card h3 {
    font-weight: 700; font-size: 32px; letter-spacing: -0.03em;
    margin-bottom: 8px; line-height: 1; position: relative;
  }
  .price-card .sub {
    color: var(--ink-2); font-size: 14px; line-height: 1.55;
    margin-bottom: 24px; position: relative;
  }
  .price-card .price {
    display: flex; align-items: baseline; gap: 6px;
    margin-bottom: 28px; padding-bottom: 28px;
    border-bottom: 1px solid var(--hairline); position: relative;
  }
  .price-card .price .num { font-weight: 700; font-size: 48px; letter-spacing: -0.04em; line-height: 1; }
  .price-card .price .unit { font-family: var(--mono); font-size: 13px; color: var(--muted); }
  .feat-list { list-style: none; margin-bottom: 28px; position: relative; }
  .feat-list li {
    padding: 8px 0; font-size: 14px; color: var(--ink-2);
    display: flex; align-items: flex-start; gap: 10px; line-height: 1.5;
  }
  .feat-list li::before {
    content: ''; width: 14px; height: 14px; border-radius: 50%;
    background: var(--alpha-soft); flex-shrink: 0; margin-top: 4px;
    background-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 14 14' fill='none'><path d='M3 7l3 3 5-6' stroke='%237cc4ff' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/></svg>");
    background-size: 14px; background-position: center; background-repeat: no-repeat;
  }
  .feat-list li.coming { opacity: 0.6; }
  .feat-list li.coming::before {
    background-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 14 14' fill='none'><circle cx='7' cy='7' r='2.5' stroke='%23a0b3cf' stroke-width='1.5'/></svg>");
  }
  .feat-list li.coming::after {
    content: 'soon'; margin-left: auto;
    font-family: var(--mono); font-size: 9px; letter-spacing: 0.1em;
    text-transform: uppercase; color: var(--muted);
  }
  .price-card .btn { width: 100%; justify-content: center; position: relative; }

  /* CTA */
  .cta-wrap {
    margin: 0 auto; max-width: 880px;
    background: linear-gradient(180deg, var(--surface) 0%, var(--bg-2) 100%);
    border: 1px solid var(--hairline); border-radius: 24px;
    padding: 72px 48px; text-align: center;
    position: relative; overflow: hidden;
  }
  .cta-wrap::before {
    content: ''; position: absolute; inset: 0;
    background: radial-gradient(800px 300px at 50% 0%, var(--alpha-soft), transparent 70%);
    pointer-events: none;
  }
  @media (max-width: 720px) { .cta-wrap { padding: 48px 24px; } }
  .cta-wrap h2 {
    font-weight: 700; font-size: clamp(36px, 5vw, 56px);
    letter-spacing: -0.035em; line-height: 1.02;
    margin-bottom: 16px; position: relative;
  }
  .cta-wrap h2 .accent { color: var(--alpha); }
  .cta-wrap p {
    color: var(--ink-2); font-size: 17px; max-width: 540px;
    margin: 0 auto 32px; line-height: 1.55; position: relative;
  }
  .waitlist {
    display: flex; gap: 8px; max-width: 480px; margin: 0 auto;
    background: var(--bg); border: 1px solid var(--hairline-2);
    border-radius: 12px; padding: 6px; position: relative;
  }
  .waitlist input {
    flex: 1; background: transparent; border: none; color: var(--ink);
    padding: 12px 16px; font-family: var(--sans); font-size: 15px; outline: none;
  }
  .waitlist input::placeholder { color: var(--muted); }
  .cta-meta {
    margin-top: 20px; font-family: var(--mono); font-size: 11px;
    color: var(--muted); letter-spacing: 0.08em; position: relative;
  }

  /* Newsletter section eyebrow — small "SUNDAY · 7PM ET" pill above the headline */
  .newsletter-eyebrow {
    margin-bottom: 24px;
    position: relative;
  }
  .newsletter-day-pill {
    display: inline-block;
    padding: 6px 12px;
    background: rgba(74, 222, 128, 0.10);
    border: 1px solid rgba(74, 222, 128, 0.30);
    border-radius: 100px;
    font-family: var(--mono);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.16em;
    color: #4ade80;
  }

  /* FOOTER */
  footer {
    border-top: 1px solid var(--hairline); padding: 64px 0 40px;
    background: var(--bg); position: relative; z-index: 2;
  }
  .foot-grid { display: grid; grid-template-columns: 1.5fr repeat(3, 1fr); gap: 48px; margin-bottom: 48px; }
  @media (max-width: 720px) { .foot-grid { grid-template-columns: 1fr 1fr; gap: 32px; } }
  .foot-col h4 {
    font-family: var(--mono); font-size: 11px; letter-spacing: 0.12em;
    text-transform: uppercase; color: var(--muted);
    margin-bottom: 16px; font-weight: 500;
  }
  .foot-col a {
    display: block; font-size: 14px; color: var(--ink-2);
    padding: 6px 0; transition: color .15s;
  }
  .foot-col a:hover { color: var(--ink); }
  .foot-bottom {
    display: flex; justify-content: space-between; align-items: center;
    padding-top: 24px; border-top: 1px solid var(--hairline);
    font-size: 12px; color: var(--muted); font-family: var(--mono);
    flex-wrap: wrap; gap: 16px;
  }
  .disclaimer {
    margin-top: 32px; padding: 20px;
    border: 1px solid var(--hairline); border-radius: 12px;
    font-size: 12px; line-height: 1.6; color: var(--muted); font-family: var(--mono);
  }

  .reveal { opacity: 0; transform: translateY(20px); transition: opacity .8s ease, transform .8s ease; }
  .reveal.in { opacity: 1; transform: translateY(0); }

  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
      animation-duration: 0.01ms !important;
      transition-duration: 0.01ms !important;
    }
  }
/* ===== DATA INTELLIGENCE section (additive, directive 2026-07-04) ===== */
#data-intel .dataintel-stats {
  display: flex; flex-wrap: wrap; gap: 28px 56px;
  margin: 40px 0 8px;
}
#data-intel .dataintel-stat { display: flex; flex-direction: column; gap: 4px; }
#data-intel .dataintel-num {
  font-size: 40px; font-weight: 800; letter-spacing: -0.02em;
  font-variant-numeric: tabular-nums; color: var(--accent, #4d9fff);
}
#data-intel .dataintel-label {
  font-size: 13px; text-transform: uppercase; letter-spacing: 0.08em; opacity: 0.7;
}
#data-intel .di-hero-row {
  display: grid; grid-template-columns: 1fr; gap: 32px 56px; margin-top: 28px;
  align-items: start;
}
@media (min-width: 900px) {
  #data-intel .di-hero-row { grid-template-columns: 1.15fr 0.85fr; }
  #data-intel .dataintel-waitlist { justify-self: end; }
}
#data-intel .dataintel-waitlist {
  max-width: 460px; width: 100%;
  background: rgba(6, 12, 24, 0.72); border: 1px solid rgba(77, 159, 255, 0.28);
  border-radius: 14px; padding: 18px;
}
#data-intel .dataintel-waitlist-label { display: block; font-size: 14px; opacity: 0.85; margin-bottom: 10px; }
#data-intel .dataintel-waitlist-row { display: flex; gap: 10px; flex-wrap: wrap; }
#data-intel .dataintel-waitlist-row input {
  flex: 1 1 200px; min-height: 46px; padding: 0 14px; border-radius: 10px;
  border: 1px solid rgba(77, 159, 255, 0.35); background: rgba(10, 22, 40, 0.6);
  color: inherit; font-size: 15px;
}
#data-intel .dataintel-waitlist-row .btn { min-height: 46px; }
#data-intel .dataintel-waitlist-msg { font-size: 13px; margin-top: 8px; min-height: 18px; opacity: 0.85; }
@media (max-width: 640px) {
  #data-intel .dataintel-num { font-size: 32px; }
  #data-intel .dataintel-stats { gap: 20px 32px; }
}

/* ===== HERO GLOBE + imagery strip (directive 2026-07-05, #data-intel only) ===== */
#data-intel { position: relative; overflow: hidden; min-height: 720px; }
#data-intel #di-globe {
  position: absolute; inset: 0; z-index: 0; pointer-events: none;
  background: radial-gradient(ellipse at 70% 40%, #0a1428 0%, #04070f 65%);
}
#data-intel #di-globe canvas { pointer-events: none !important; }
#data-intel .di-globe-shade {
  position: absolute; inset: 0; z-index: 1; pointer-events: none;
  /* hero-refinements 2026-07-05: the globe is the centerpiece — the shade
     only protects text legibility, it no longer curtains the sphere */
  background: linear-gradient(90deg, rgba(4,7,15,0.72) 0%, rgba(4,7,15,0.34) 55%, rgba(4,7,15,0.04) 100%);
}
@media (max-width: 900px) {
  #data-intel .di-globe-shade {
    background: linear-gradient(180deg, rgba(4,7,15,0.72) 0%, rgba(4,7,15,0.42) 100%);
  }
}
#data-intel .container { position: relative; z-index: 2; }
#data-intel .di-imagery {
  display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 18px; margin-top: 44px;
}
#data-intel .di-imagery-card { margin: 0; }
#data-intel .di-imagery-card img {
  width: 100%; height: 170px; object-fit: cover; border-radius: 12px;
  border: 1px solid rgba(77, 159, 255, 0.25); display: block;
  background: #0a1428;
}
#data-intel .di-imagery-card figcaption {
  font-size: 12px; line-height: 1.5; opacity: 0.75; margin-top: 8px;
}
#data-intel .di-imagery-credit {
  grid-column: 1 / -1; font-size: 11px; opacity: 0.55; margin: 2px 0 0;
}
