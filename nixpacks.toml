[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "python3 -m pip install yfinance scipy numpy --quiet"
]

[start]
cmd = "npm run build && node dist/index.cjs"
