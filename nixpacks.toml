[phases.setup]
nixPkgs = ["nodejs_20", "python312"]

[phases.install]
cmds = [
  "npm install",
  "python3 -m pip install yfinance scipy numpy --break-system-packages --quiet"
]

[start]
cmd = "node dist/index.cjs"
